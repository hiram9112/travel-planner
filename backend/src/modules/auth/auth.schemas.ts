/**
 * Authentication validation schemas.
 * These schemas define the expected shape of auth-related inputs.
 * Validation logic will be added later.
 */

export const registerSchema = {
  // TODO: define register validation schema
};

export const loginSchema = {
  // TODO: define login validation schema
};
